info:
  name: albums
  type: folder
  seq: 2

request:
  auth: inherit
  scripts:
    - type: after-response
      code: |-
        test("Status code is 200", function() {
            expect(res.getStatus()).to.equal(200);
          });
